package com.example.crawler

import com.example.store.MockLottoHistoryStore
import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Test

@DisplayName("Crawler")
class CrawlerTest {

    /*@DisplayName("should save all draws since 1971 when store is empty")
    @Test
    fun shouldFetchAllWhenEmpty() {
        val mockStore = MockLottoHistoryStore()
        val crawler = Crawler(mockStore)

        crawler.scrapeAndSaveDrawsSinceLastSavedDraw()
    }*/

}